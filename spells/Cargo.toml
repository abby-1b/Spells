[package]
name = "spells"
version = "2.0.1"
edition = "2021"

[dependencies]
tiny_http = "0.11"
rstsc = {path = "../../rstsc/rstsc"}
markdown = "1.0.0"

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"
